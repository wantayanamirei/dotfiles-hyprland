#!/bin/bash

while true; do
    # Ваша команда (например, вывод времени в файл)
    ./themeA.sh
    # Пауза 10 минут (600 секунд)
    sleep 600
    # Ваша команда (например, вывод времени в файл)
    ./themeB.sh
    # Пауза 10 минут (600 секунд)
    sleep 600
    # Ваша команда (например, вывод времени в файл)
    ./themeC.sh
    # Пауза 10 минут (600 секунд)
    sleep 600
done